[project]
name = "pwv-with-mvo-python"
version = "0.1.0"
description = "Work-in-progress runtime experiments for module-level multi-version objects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "work-in-progress/src"}

[tool.setuptools.packages.find]
where = ["work-in-progress/src"]

[dependency-groups]
dev = [
  "pytest>=8.4.2",
]
bench = [
  "matplotlib>=3.8",
]
